name: Claude Code

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  issues:
    types: [opened, assigned]
  pull_request_review:
    types: [submitted]

jobs:
  claude:
    # Run only on a literal @claude mention AND only from a trusted actor — the repo owner,
    # an org member, or an invited collaborator. These events execute in the base-repo context
    # with secrets and a write token even when they come from a fork PR, so gating on author
    # association here stops an outside contributor from starting a privileged, API-key-consuming
    # job. (The action enforces the same permission check internally; this refuses the run before
    # it ever spins up.)
    #
    # The gate covers who *starts* the job, not whose text it reads: invoking @claude on a fork's
    # pull request still feeds an outsider's diff and body to a job holding contents:write and the
    # API key. That is left to the maintainer's judgement rather than an `if:`, because the trigger
    # this arrives through is `issue_comment`, whose payload describes an issue and carries no
    # head repository — there is no fork flag to test. A condition that only covered the two
    # pull_request_review* triggers would read like a guarantee while the common path stayed open.
    if: |
      (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@claude') && contains(fromJSON('["OWNER", "MEMBER", "COLLABORATOR"]'), github.event.comment.author_association)) ||
      (github.event_name == 'pull_request_review_comment' && contains(github.event.comment.body, '@claude') && contains(fromJSON('["OWNER", "MEMBER", "COLLABORATOR"]'), github.event.comment.author_association)) ||
      (github.event_name == 'pull_request_review' && contains(github.event.review.body, '@claude') && contains(fromJSON('["OWNER", "MEMBER", "COLLABORATOR"]'), github.event.review.author_association)) ||
      (github.event_name == 'issues' && (contains(github.event.issue.body, '@claude') || contains(github.event.issue.title, '@claude')) && contains(fromJSON('["OWNER", "MEMBER", "COLLABORATOR"]'), github.event.issue.author_association))
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write
      id-token: write
      actions: read # Required for Claude to read CI results on PRs
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: Run Claude Code
        id: claude
        uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
