{
  "name": "@redduck/payload-config",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "generate": "yarn generate:types && yarn generate:schema",
    "generate:types": "payload generate:types",
    "generate:schema": "payload generate:db-schema && node ./scripts/fix-payload-generated-schema-imports.mjs",
    "generate:schema:payload-only": "payload generate:db-schema"
  },
  "dependencies": {
    "@payloadcms/db-postgres": "3.80.0",
    "@payloadcms/richtext-lexical": "3.80.0",
    "@payloadcms/storage-s3": "3.80.0",
    "dotenv": "^17.3.1",
    "drizzle-orm": "^0.45.1",
    "payload": "3.80.0"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
